import { DealInputScreen } from '@/screens/DealInput';
import { type NavigationProps } from '../App';

export const DealInput = (navProps: NavigationProps) => {
  return <DealInputScreen {...navProps} />;
};