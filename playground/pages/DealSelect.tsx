import { DealSelectScreen } from '@/screens/DealSelect';

import { type NavigationProps } from '../App';

export const DealSelect = (navProps: NavigationProps) => {
  return <DealSelectScreen {...navProps} />;
};
